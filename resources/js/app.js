import './bootstrap';
const $=(s,p=document)=>p.querySelector(s), $$=(s,p=document)=>[...p.querySelectorAll(s)];
const header=$('[data-header]'), hero=$('.hero'); const syncHeader=()=>{if(!header)return;header.classList.toggle('scrolled',scrollY>30);header.classList.toggle('light-header',!!hero&&scrollY>hero.offsetHeight-100)}; syncHeader(); window.addEventListener('scroll',syncHeader,{passive:true});
const toggle=$('[data-menu-toggle]'), menu=$('[data-menu-overlay]'), close=$('[data-menu-close]'); let menuReturnFocus=null;
const menuFocusables=()=>$$('a[href],button:not([disabled])',menu); const setMenu=(open)=>{if(!menu||!toggle)return;menu.hidden=!open;menu.classList.toggle('menu-compact',open&&window.innerWidth>900);menu.classList.remove('menu-expanded');toggle.setAttribute('aria-expanded',String(open));document.body.classList.toggle('menu-is-open',open);if(open){menuReturnFocus=document.activeElement;requestAnimationFrame(()=>menuFocusables()[0]?.focus())}else{menu.classList.remove('menu-compact');menuReturnFocus?.focus();menuReturnFocus=null}};
toggle?.addEventListener('click',()=>setMenu(true));close?.addEventListener('click',()=>setMenu(false));$$('[data-menu-overlay] a').forEach(a=>a.addEventListener('click',()=>setMenu(false)));
const activatePanel=(key,source)=>{menu?.classList.remove('menu-compact');menu?.classList.add('menu-expanded');$$('[data-menu-category]').forEach(b=>{const active=b.dataset.menuCategory===key;b.classList.toggle('active',active);b.setAttribute('aria-expanded',String(active))});$$('[data-menu-panel]').forEach(panel=>{const active=panel.dataset.menuPanel===key;panel.hidden=!active;panel.classList.toggle('active',active);if(active){const image=$('[data-menu-featured]',panel);if(image?.dataset.defaultImage)image.src=image.dataset.defaultImage}});source?.focus()};
$$('[data-menu-image]').forEach(link=>{const updateImage=()=>{const panel=link.closest('[data-menu-panel]'),image=panel&&$('[data-menu-featured]',panel);if(image&&link.dataset.menuImage){image.src=link.dataset.menuImage;image.alt=link.dataset.menuImageAlt||image.alt}};link.addEventListener('mouseenter',updateImage);link.addEventListener('focus',updateImage)});
$$('[data-menu-category]').forEach(button=>{button.addEventListener('click',()=>activatePanel(button.dataset.menuCategory,button))});
document.addEventListener('keydown',e=>{if(!menu||menu.hidden)return;if(e.key==='Escape'){e.preventDefault();setMenu(false)}if(e.key==='Tab'){const items=menuFocusables();if(!items.length)return;const first=items[0],last=items[items.length-1];if(e.shiftKey&&document.activeElement===first){e.preventDefault();last.focus()}else if(!e.shiftKey&&document.activeElement===last){e.preventDefault();first.focus()}}});
const modal=$('[data-tour-modal]'); const closeModal=()=>{if(modal){modal.hidden=true;document.body.style.overflow=''}}; $$('[data-tour-open]').forEach(b=>b.addEventListener('click',()=>{modal.hidden=false;document.body.style.overflow='hidden';$('[data-tour-close]',modal)?.focus()})); $$('[data-tour-close]').forEach(b=>b.addEventListener('click',closeModal)); document.addEventListener('keydown',e=>{if(e.key==='Escape'){closeModal();$('[data-lightbox]')?.setAttribute('hidden','')}});
const banner=$('[data-cookie-banner]'); if(localStorage.getItem('ohh-cookie-choice')) banner?.remove(); $$('[data-cookie]').forEach(b=>b.addEventListener('click',()=>{localStorage.setItem('ohh-cookie-choice',b.dataset.cookie);banner?.remove()}));
const filters=$('[data-gallery-filters]'), items=$$('[data-gallery] .gallery-item'); filters&&$$('button',filters).forEach(btn=>btn.addEventListener('click',()=>{ $$('button',filters).forEach(b=>b.classList.remove('active'));btn.classList.add('active');const f=btn.dataset.filter;items.forEach(i=>i.hidden=f!=='all'&&i.dataset.category!==f)}));
const lightbox=$('[data-lightbox]'); items.forEach(i=>i.addEventListener('click',()=>{lightbox.hidden=false;$('[data-lightbox-title]').textContent=i.dataset.title;document.body.style.overflow='hidden'})); $('[data-lightbox-close]')?.addEventListener('click',()=>{lightbox.hidden=true;document.body.style.overflow=''});
